# امنیت

## گزارش آسیب‌پذیری

اگر مشکل امنیتی پیدا کردید، آن را به‌صورت عمومی در Issue نگذارید. ترجیحاً با نگهدارندهٔ مخزن به‌صورت خصوصی تماس بگیرید و جزئیات بازتولید را بفرستید.

## نکات توسعه

- **`JWT_SECRET` و `LLM_TOKEN_ENCRYPTION_KEY`** را در محیط واقعی تصادفی و طولانی بگذارید؛ مقادیر نمونهٔ `.env.example` را در production استفاده نکنید.
- جلسهٔ ورود ۱۵ دقیقه بی‌فعالیتی اعتبار دارد؛ با استفادهٔ کاربر تمدید می‌شود. توکن JWT هم حداکثر ۱۵ دقیقه است.
- فایل‌های `.env` و دیتابیس محلی (`*.db`) در git نیستند؛ آن‌ها را منتشر نکنید.
- توکن LLM کاربر نباید به‌صورت plaintext در لاگ یا `AiTrace` عمومی دیده شود.
- آپلود PDF (`funds`) را به نوع/اندازهٔ معقول محدود کنید و مسیر فایل را از ورودی کاربر نسازید.
- برای Docker، فقط nginx را روی ۸۰/۴۴۳ عمومی کنید؛ پورت‌های وب/API و دیتابیس را روی اینترنت expose نکنید. API بدون Docker فقط روی `127.0.0.1` گوش می‌دهد.
- `JWT_SECRET` و رمز Postgres را از مقادیر نمونه عوض کنید.

## مسئولیت محصول

سبدیار ابزار کمکی است و **مشاورهٔ سرمایه‌گذاری رسمی** ارائه نمی‌دهد. از ادعاهای قطعی بازده یا توصیهٔ خرید/فروش به‌عنوان حقیقت مالی خودداری کنید.
