# معماری — سبدیار

## نمای کلی

```
┌─────────────────┐                      ┌─────────────────┐
│ مرورگر          │  sabad-yar.ir        │ nginx :80/:443  │
│                 │─────────────────────►│ reverse proxy   │
└─────────────────┘                      └───┬─────────┬───┘
                                             │         │
                              /          :3000│         │/api :3001
                                             ▼         ▼
                                      ┌──────────┐ ┌──────────┐
                                      │ apps/web │ │ apps/api │
                                      │ Next.js  │ │ NestJS   │
                                      └────┬─────┘ └────┬─────┘
                                           │            │ Prisma
                                           └──────┬─────┘
                                                  ▼
                                         PostgreSQL (داخلی)
```

اختیاری: LLM سازگار با OpenAI (پلتفرم یا توکن کاربر) و اینجست دادهٔ بازار.

## لایه‌ها

### وب (`apps/web`)

- App Router؛ شِل مشترک در `AppShell`.
- احراز هویت سمت کلاینت با `localStorage` کلید `sabadyar_token`.
- صفحات اصلی: داشبورد، سبدها، بازار، صندوق‌ها، اخبار اقتصادی، اقتصاد ایران، اقتصاد دنیا، آزمایش فارکس، درس‌آموخته‌ها، تنظیمات، ورود/ثبت‌نام.

### API (`apps/api`)

| ماژول | مسئولیت |
|--------|---------|
| `auth` | ثبت‌نام / ورود JWT، ورود زیست‌سنجی WebAuthn (اثر انگشت / چهره روی موبایل) |
| `users` | پروفایل ریسک و افق زمانی |
| `market` | نقل‌قول‌ها، اینجست قیمت/EPS/P/E، گفتگوی LLM فقط دربارهٔ بورس تهران |
| `portfolios` | CRUD سبد، پیشنهاد، بازچینش، ارزیابی ماهانه، نقد؛ افزودن/ویرایش موقعیت با تعداد یا مبلغ (آخرین قیمت دیتابیس) |
| `llm` | تنظیمات مدل و توکن رمزنگاری‌شده |
| `funds` | آپلود PDF گزارش صندوق و تحلیل |
| `macro` | تورم، بهره، ریسک ژئوپلیتیک، پرسش |
| `news` | اخبار و فرصت‌های خرد روزانه، زمینهٔ پیشنهاد سبد |
| `telegram` | ربات تلگرام [`https://t.me/sabadyaar_bot`](https://t.me/sabadyaar_bot): وصل با موبایل پروفایل، پیام در روز و ساعت انتخابی ادمین + فایل صوتی اخبار با صدای زن |
| `telegram-assistant` | دستیار دوطرفه تلگرام: پرسش LLM، ترجمه/خلاصه صفحه، PDF فارسی RTL، صوت یوتیوب |
| `world-markets` | اقتصاد دنیا: بیت‌پین + اختلاف قیمت رمزارز با یاهو فایننس + ۵ خبر کلان جهان/آمریکا |
| `forex` | آزمایش گراف فارکس (`/forex` فقط admin): نرخ جفت‌ارز + BTC/ETH/طلا/نقره، ذخیرهٔ رأس/یال با زمان |
| `lessons` | درس‌آموخته‌ها؛ آپلود PDF اقتصاد ایران و استخراج با LLM |
| `prices` | قیمت دلار و طلا (پیش‌فرض بیت‌پین؛ API سفارشی اختیاری) |

پیشوند سراسری: `/api`. CORS از `CORS_ORIGIN`. ورود عمومی فقط از nginx روی دامنهٔ `sabad-yar.ir`.

### داده (`apps/api/prisma/schema.prisma`)

موجودیت‌های کلیدی: `User`، `UserProfile` (موبایل و اتصال تلگرام)، `WebAuthnCredential`، `Portfolio`، `PortfolioSnapshot` / `SnapshotItem`، `Instrument` / `PriceBar`، `FundReport` / `FundHolding`، `MarketChatMessage`، `Lesson`، `MacroSnapshot`، `EconomicNewsBatch` / `EconomicNewsItem`، `TelegramBotConfig`، `TelegramAssistantConfig`، `WorldMarket` / `WorldMarketRefresh` / `WorldMacroNews`، `ForexSnapshot` / `ForexGraphNode` / `ForexGraphEdge`، `AiTrace`، `LlmSetting`.

اسنپ‌شات‌ها تاریخچهٔ پیشنهاد و بازچینش را نگه می‌دارند؛ رویدادها در `PortfolioEvent`.

### مشترک (`packages/shared`)

Enumها و DTOهای پیشنهاد سبد + لیبل‌های فارسی استراتژی و نوع دارایی. منبع حقیقت برای قرارداد وب↔API.

## جریان‌های مهم

1. **پیشنهاد سبد:** پروفایل + سرمایه + استراتژی + زمینهٔ بازار/کلان → LLM → `PortfolioSnapshot` از نوع `SUGGESTION` با `reasonFa` برای هر آیتم.
2. **بازار:** اینجست دوره‌ای/دستی → `Instrument` و `PriceBar`. گفتگوی صفحهٔ بازار فقط با دادهٔ همین نمادها و `FundHolding` پاسخ می‌دهد؛ سؤال نامرتبط رد می‌شود.
3. **صندوق:** آپلود PDF → استخراج متن → امتیاز/استراتژی حدسی → اختیاری در پیشنهادها.
4. **درس‌آموخته از اقتصاد ایران:** ادمین PDF را در `/lessons` بارگذاری می‌کند → متن استخراج می‌شود → LLM درس‌های مفید برای سبد سهام را برمی‌گرداند → ردیف‌های `Lesson` با منبع `iran_economy_pdf` ذخیره می‌شوند و در پیشنهاد/بازچینش سبد به مدل داده می‌شوند.
5. **اخبار و فرصت‌ها:** در ساعتی که ادمین برای اخبار ایران ذخیره کرده (پیش‌فرض ۸:۰۰ تهران، تلاش مجدد یک و دو ساعت بعد اگر خالی بماند) مدل Grok با ابزار جستجوی زندهٔ کل شبکهٔ X (هر زبان) حداکثر ۷ مطلب از حساب‌های معتبر اثرگذار بر اقتصاد ایران و حداکثر ۳ فرصت سرمایه‌گذاری (عرضه اولیه، خودرو، حراج سکه/ارز، آربیتراژ، بازار مستعد رشد) را به فارسی استخراج و در `EconomicNewsItem` ذخیره می‌کند. همین موارد در پیشنهاد و بهبود سبد به مدل داده می‌شوند. در روزها و ساعتی که ادمین در تنظیمات ربات ذخیره کرده (پیش‌فرض هر روز ۸:۳۰ تهران، تلاش مجدد ۱۵ دقیقه بعد) به هر کاربر وصل‌شده تلگرام، نمودار سبد جاری و پیشنهاد بهبود همان سبد فرستاده می‌شود. متن اخبار و فایل صوتی هر روز فقط یک‌بار ساخته و برای همه ارسال می‌شود؛ مدل برای هر کاربر فقط ترکیب فعلی سبد و پیشنهاد بهبود را می‌بیند. همان اخبار و فرصت‌ها با پرامپت `telegram_digest_voice` به متن گفتاری فارسی روان تبدیل و با `speakTts` به mp3 با صدای زن (پیش‌فرض `nova`، حداکثر حدود دو دقیقه) درمی‌آید و با `sendAudio` بعد از متن ارسال می‌شود؛ اگر مدل یا سرویس گفتار خطا داد، متن آمادهٔ `digest-voice.ts` جایگزین می‌شود و در بدترین حالت فقط پیام متنی می‌رود. ربات عمومی:

```
https://t.me/sabadyaar_bot
```
6. **اقتصاد دنیا:** در ساعتی که ادمین برای اقتصاد دنیا ذخیره کرده (پیش‌فرض ۷:۰۰ تهران) همهٔ بازارهای بیت‌پین خوانده و در `WorldMarket` جایگزین می‌شوند. قیمت هر رمزارز با یاهو فایننس مقایسه و اختلاف در همان کارت نماد نشان داده می‌شود. کلیک روی کارت، نمودار تاریخی همان نماد را مثل بازار تهران باز می‌کند (`GET /world-markets/:code/history`). نشانی API بیت‌پین و یاهو در تنظیمات ادمین (`WorldSourceApiConfig`) ذخیره می‌شود و اگر مدیر عوضشان کند، خواندن بعدی از همان آدرس‌هاست. مدیر می‌تواند از صفحهٔ ادمین فوری همگام کند. در ادامهٔ همان همگام‌سازی (خودکار یا فوری)، پرامپت `world_macro_news` با جستجوی زندهٔ خبر حداکثر ۵ خبر مهم اقتصاد کلان جهان و به‌ویژه آمریکا را که روی نفت، طلا، دلار، فلزات پایه یا رمزارز اثر دارد از رسانه‌های معتبر می‌خواند و در `WorldMacroNews` جایگزین می‌کند (خلاصه و فهرست منابع در `WorldMarketRefresh`). این اخبار در صفحهٔ `/world` مثل اخبار ایران نمایش داده می‌شوند، `worldMacroNews` در زمینهٔ پیشنهاد و آنالیز سبد به مدل داده می‌شود (فقط اثر ایران ملاک تصمیم است)، و درس‌های پایدار هر خبر با منبع `world_macro_news:<dateKey>` به `Lesson` اضافه می‌شوند.
7. **آزمایش فارکس:** نرخ جفت‌ارزهای معروف به‌علاوه بیت‌کوین، اتریوم، طلا و نقره خوانده می‌شود، گراف رأس/یال با زمان ذخیره می‌گردد، و برای هر مسیر سود خالص پس از اسپرد، کمیسیون، لغزش، سواپ و تأخیر اجرا حساب می‌شود. پیشنهاد معامله فقط اگر خالص مثبت باشد.
8. **ورود زیست‌سنجی:** روی گوشی و HTTPS دامنه، پس از ورود با رمز می‌توان اثر انگشت یا چهرهٔ همان دستگاه را ثبت کرد؛ ورود بعدی با WebAuthn و بدون رمز است.

## زمان‌بندی داخل API

کرون‌ها با `@nestjs/schedule` داخل پروسهٔ API اجرا می‌شوند؛ کانتینر `api` باید روشن بماند.

| ساعت تهران | کار |
|------------|-----|
| ساعت اقتصاد دنیا | بیت‌پین + اخبار کلان جهان/آمریکا + ذخیره دلار آزاد و طلای ۱۸ عیار (پیش‌فرض ۰۷:۰۰، تلاش مجدد یک و دو ساعت بعد) |
| ساعت اخبار ایران | اخبار و فرصت‌های خرد (پیش‌فرض ۰۸:۰۰، تلاش مجدد یک و دو ساعت بعد اگر خالی ماند) |
| ساعت انتخابی ادمین | تلگرام شخصی: نمودار سبد + پیشنهاد AI + اخبار + فایل صوتی (پیش‌فرض ۰۸:۳۰، تلاش مجدد ۱۵ دقیقه بعد، فقط روزهای انتخاب‌شده) |
| ۱۲:۰۰ | تکرار قیمت دلار و طلا از بیت‌پین |
| هر ۵ دقیقه | آزمایش گراف فارکس (نرخ جفت‌ارز و فلز/رمزارز) |
| ۲۲:۰۰ | اینجست بازار TSETMC |

## استقرار

- **توصیه:** `docker compose up -d --build` — سرویس‌های `nginx`، `web`، `api`، `db`.
- دامنهٔ عمومی: `https://sabad-yar.ir` — API از مرورگر: همان origin + `/api` (مثلاً `http://46.249.100.230/api`، بدون پورت ۳۰۰۱).
- کلاینت وب host:port را در باندل قفل نمی‌کند؛ `NEXT_PUBLIC_API_URL` در بیلد خالی است.
- `CORS_ORIGIN` باید origin همان UI باشد (دامنه یا IP، بدون `:3000` / `:3001`).
- پورت‌های `3000` و `3001` روی هاست publish نمی‌شوند؛ فقط `80` و `443`.
- بدون Docker، API روی `127.0.0.1` گوش می‌دهد (`API_LISTEN_HOST`).
- آپلودها در volume با نام `sabadyar_uploads` ماندگار می‌مانند.
- schema پیش‌فرض Prisma: `postgresql`.
