# Changelog

فرمت بر پایهٔ [Keep a Changelog](https://keepachangelog.com/fa/1.1.0/).
نسخه‌دهی معنایی: [SemVer](https://semver.org/lang/fa/).

## [Unreleased]

### Added

- آزمایش گراف فارکس در `/forex` (فقط ادمین): سود خالص پس از اسپرد، کمیسیون، لغزش، سواپ و تأخیر اجرا؛ پیشنهاد معامله فقط اگر خالص مثبت باشد
- گفتگوی LLM در صفحهٔ بازار سهام تهران: فقط نماد/شاخص بورس تهران با دادهٔ قیمت و صندوق‌هایی که سهم را خریده‌اند
- اقتصاد دنیا: همگام‌سازی بیت‌پین؛ اختلاف قیمت رمزارز با یاهو فایننس روی کارت نماد
- قیمت دلار آزاد و طلای ۱۸ عیار از بیت‌پین (تتر و انس طلای دیجیتال) در اقتصاد ایران
- دستیار تلگرام دوطرفه در تنظیمات ادمین: پرسش فارسی، ترجمه/خلاصه لینک، PDF راست‌چین، صوت یوتیوب با `gpt-4o-mini-tts`
- ربات تلگرام ۸:۳۰ از [`https://t.me/sabadyaar_bot`](https://t.me/sabadyaar_bot): نمودار سبد جاری، پیشنهاد بهبود با دادهٔ بورس/ارز/اخبار، و فرصت‌های خرد
- GitHub Actions: استقرار خودکار سرور تست (`.github/workflows/deploy-test.yml`) + اسکریپت `scripts/deploy-test-server.sh`
- استک کامل Docker (`docker-compose.yml` + `Dockerfile.api` / `Dockerfile.web` + nginx reverse proxy) برای دامنهٔ `sabad-yar.ir`
- نمونه env سرور: `docker/env.production.example` (دامنه `sabad-yar.ir`)
- مستندات استاندارد مخزن: `AGENTS.md`، `ARCHITECTURE.md`، `CONTRIBUTING.md`، `SECURITY.md`
- درس‌آموخته‌ها: آپلود PDF اقتصاد ایران، استخراج با LLM و ذخیره برای پیشنهاد بهتر سبد
- ورود موبایل با اثر انگشت یا تشخیص چهره (WebAuthn / کلید عبور دستگاه)

### Changed

- اقتصاد دنیا: حذف ۵ سیگنال X؛ نمایش «اختلاف با بازار جهانی» نسبت به یاهو فایننس روی هر رمزارز
- PDF دستیار تلگرام: فونت فارسی شکل‌دهی‌شده، اعداد برعکس نمی‌شوند، اندازهٔ صفحه از سند اصلی و جدول واقعی
- دکمهٔ «انجام شود» روی هر پیشنهاد بهبود آنالیز، همان اقدام را روی سبد اعمال می‌کند
- دکمهٔ «پیشنهاد سریع» از صفحهٔ سبد برداشته شد
- دکمهٔ بازچینش فقط پس از نتیجهٔ آنالیز AI و پیشنهاد چند استراتژی (نه در نوار بالای صفحهٔ سبد)
- گفتگوی سبد: وضعیت سهام، دلار و طلا حتی اگر در سبد نباشند، از آخرین قیمت دیتابیس
- افزودن و ویرایش نماد سبد با تعداد سهم یا مبلغ کل ریال؛ تبدیل از آخرین قیمت دیتابیس (وزن٪ فقط نمایشی)
- اخبار اقتصادی ایران: جستجوی کل فضای X با Grok؛ حداکثر ۷ خبر اثرگذار و ۳ فرصت سرمایه‌گذاری، ذخیره در دیتابیس و لحاظ در پیشنهاد سبد
- فونت PDF دستیار تلگرام از فایل کوچک Vazirmatn داخل مخزن؛ بدون دانلود jsDelivr از کل ریپوی فونت
- استخراج متن یوتیوب در دستیار تلگرام: Innertube + صفحهٔ watch + Invidious؛ صدا اگر زیرنویس نباشد
- Prisma datasource پیش‌فرض: PostgreSQL (هماهنگ با Docker)
- ورود عمومی از nginx روی `sabad-yar.ir`؛ API از مسیر `/api`؛ پورت‌های ۳۰۰۰ و ۳۰۰۱ روی اینترنت publish نمی‌شوند
- nginx بعد از recreate شدن api/web نام را دوباره از DNS داکر می‌گیرد (دیگر ۵۰۲ به‌خاطر IP کهنه)

## [0.1.0] — 2026-08

### Added

- مونوریپو اولیه: وب Next.js، API NestJS+Prisma، پکیج shared
- احراز هویت چندکاربره و سبدهای جدا
- پیشنهاد/بازچینش سبد با LLM، بازار، صندوق‌ها، کلان، درس‌آموخته‌ها
- SQLite برای توسعه؛ Docker Compose برای PostgreSQL اختیاری
